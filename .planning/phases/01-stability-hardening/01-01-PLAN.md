---
phase: 01-stability-hardening
plan: 01-01
type: cleanup
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/prakash/semora/data/remote/FirestoreSemesterRepository.kt
  - app/src/main/java/com/example/semora/ui/auth/PinVerificationActivity.kt
  - app/src/main/java/com/prakash/semora/ui/home/HomeViewModel.kt
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - build.gradle.kts (root)
  - settings.gradle.kts
  - app/src/main/java/com/example/semora/SemoraApplication.kt
  - app/src/main/java/com/prakash/semora/data/local/AppDatabase.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/UserDao.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/SemesterDao.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/GradeDao.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt
autonomous: true
requirements: [D-13, D-14, D-15, D-17, D-18]
---

<objective>
Remove all dead code: preload mechanism, Room layer, KSP config, and dead MutableStateFlow. Fix FirestoreProfileRepository to use .set(merge()) instead of .update() to prevent crash on missing doc.
</objective>

<tasks>

<task id="01-01-01" type="refactor">
<read_first>
- FirestoreSemesterRepository.kt (lines 26-42)
- PinVerificationActivity.kt (line 137)
- HomeViewModel.kt (lines 61-68)
</read_first>
<action>
Remove preload mechanism:

1. In FirestoreSemesterRepository.kt:
   - Delete `@Volatile var preloadedSemesters` field (line 27)
   - Delete `preload()` method (lines 30-36)
   - Delete `consumePreloaded()` method (lines 38-42)

2. In PinVerificationActivity.kt:
   - Delete line 137: `FirestoreSemesterRepository.preload(deviceUid, profile.id)`
   - Remove unused import of FirestoreSemesterRepository (line 18) if no other usage

3. In HomeViewModel.kt:
   - Delete the preloaded consume block (lines 61-68)
   - Delete the unused MutableStateFlow field if it exists (the dead StateFlow flagged in CODE_QUALITY.md)
   - Clean up any unused imports

4. In FirestoreProfileRepository.kt:
   - Change `updateUsername()` (line 50): `.update("username", username)` → `.set(mapOf("username" to username), SetOptions.merge())`
   - Change `updatePin()` (lines 54-60): `.update(mapOf(...))` → `.set(mapOf(...), SetOptions.merge())`
   - Add `import com.google.firebase.firestore.SetOptions` if not present
</action>
<acceptance_criteria>
- No references to `preload()` or `consumePreloaded()` remain in codebase
- PinVerificationActivity still compiles and navigates to MainActivity
- HomeViewModel compiles without unused field warnings
- All preload-related tests removed (no tests exist, but build passes)
- FirestoreProfileRepository.updateUsername and updatePin use .set(SetOptions.merge())
</acceptance_criteria>
<verify>
1. Grep for "preload" and "consumePreloaded" — zero matches expected
2. Grep for "MutableStateFlow" in HomeViewModel — should be zero (only StateFlow in SemViewModel)
3. Check FirestoreProfileRepository has `import com.google.firebase.firestore.SetOptions`
4. `./gradlew assembleDebug` succeeds
</verify>
</task>

<task id="01-01-02" type="cleanup">
<read_first>
- gradle/libs.versions.toml (lines 28-30)
- app/build.gradle.kts (lines 35-37, 47-50)
- build.gradle.kts (root)
- settings.gradle.kts
- SemoraApplication.kt (lines 10, 54-133)
- app/src/main/java/com/prakash/semora/data/local/ (entire directory)
</read_first>
<action>
Remove Room dependencies and migration code:

1. In gradle/libs.versions.toml:
   - Comment out line 28: `# androidx-room-runtime = ...`
   - Comment out line 29: `# androidx-room-ktx = ...`
   - Comment out line 30: `# androidx-room-compiler = ...`
   - Keep the `room = "2.8.4"` version entry in [versions] for reference (can be removed later)

2. In app/build.gradle.kts:
   - Comment out lines 35-37 (KSP arg block with `room.schemaLocation`)
   - Comment out lines 47-50 (Room dependencies block)

3. In root build.gradle.kts and settings.gradle.kts:
   - No changes needed (KSP plugin is a Gradle plugin applied in app module, removing room-compiler dep makes KSP a no-op for Room)
   - Keep `google-devtools-ksp` plugin declaration for now (removing it requires verifying nothing else uses KSP)

4. In SemoraApplication.kt:
   - Remove line 10: `import com.prakash.semora.data.local.AppDatabase`
   - Remove the entire `runMigration()` method (lines 54-133)
   - Remove `applicationScope` field (line 22) and its import (`kotlinx.coroutines.CoroutineScope` etc.)
   - Simplify `initFirebase()` to just call `FirestoreAuthRepository.ensureSignedIn()` without migration logic
   - Remove unused imports (CoroutineScope, Dispatchers, SupervisorJob, launch, tasks.await, FirestoreProfileRepository, ProfileDoc)

5. Delete entire `app/src/main/java/com/prakash/semora/data/local/` directory:
   - AppDatabase.kt
   - UserDao.kt
   - SemesterDao.kt
   - GradeDao.kt
</action>
<acceptance_criteria>
- `./gradlew assembleDebug` succeeds with no Room-related compile errors
- No references to `AppDatabase`, `UserDao`, `SemesterDao`, `GradeDao` in codebase
- SemoraApplication runs without migration code on startup
- data/local/ directory no longer exists
</acceptance_criteria>
<verify>
1. Grep for "AppDatabase" — only false positives in git history
2. Grep for "room" in build.gradle.kts — only commented lines
3. Grep for "room" in libs.versions.toml — only version ref and commented library entries
4. Test-Path "app/src/main/java/com/prakash/semora/data/local" — false
5. `./gradlew assembleDebug` succeeds
</verify>
</task>

</tasks>

<verification>
1. Build the project: `./gradlew assembleDebug` must pass
2. Run the app: create a profile, enter grades, switch tabs — no crashes
3. Go offline, verify cached data shows, no crash
4. Edit profile name, verify save works
5. Edit PIN, verify save works
</verification>

<success_criteria>
- Zero preload-related code remains
- Room references removed from build config and source
- FirestoreProfileRepository uses .set(merge()) exclusively
- `./gradlew assembleDebug` succeeds
- App starts and core flow (auth → grades → dashboard) works
</success_criteria>
