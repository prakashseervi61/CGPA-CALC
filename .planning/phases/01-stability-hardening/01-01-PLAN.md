---
phase: 01-stability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/prakash/semora/data/remote/FirestoreSemesterRepository.kt
  - app/src/main/java/com/prakash/semora/ui/auth/PinVerificationActivity.kt
  - app/src/main/java/com/prakash/semora/ui/home/HomeViewModel.kt
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - build.gradle.kts
  - app/src/main/java/com/prakash/semora/data/local/ (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/AppDatabase.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/UserDao.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/SemesterDao.kt (DELETE)
  - app/src/main/java/com/prakash/semora/data/local/GradeDao.kt (DELETE)
  - app/src/main/java/com/example/semora/SemoraApplication.kt
  - app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt
autonomous: true
requirements:
  - "Offline resilience (preload removal)"
  - "Profile-edit crash fix"
  - "Remove dead Room code"
  - "Remove dead HomeViewModel MutableStateFlow"

must_haves:
  truths:
    - "D-18: FirestoreSemesterRepository no longer exposes preload() or consumePreloaded()"
    - "D-18: PinVerificationActivity no longer calls preload()"
    - "D-18: HomeViewModel no longer references consumePreloaded()"
    - "D-13/D-14/D-15: No Room imports, classes, or plugin config remain in the build"
    - "D-14: app/src/main/java/com/prakash/semora/data/local/ directory no longer exists"
    - "D-17: No dead MutableStateFlow in HomeViewModel"
    - "D-19: HomeViewModel cachedDashboard SWR pattern preserved as-is (already present)"
    - "FirestoreProfileRepository.updateUsername and updatePin use .set(SetOptions.merge()) — resolves profile-edit crash"
    - "D-14/D-15: SemoraApplication no longer has Room import, runMigration(), or applicationScope"
    - "Build compiles and runs without Room references"
  artifacts:
    - path: "app/src/main/java/com/prakash/semora/data/remote/FirestoreSemesterRepository.kt"
      provides: "Clean repository without preload pattern"
      not_contains: ["preloadedSemesters", "fun preload", "fun consumePreloaded"]
    - path: "app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt"
      provides: "Profile repository with .set(merge()) instead of .update()"
      contains: ["SetOptions.merge"]
    - path: "gradle/libs.versions.toml"
      provides: "Version catalog without Room entries"
      not_contains: ["room-runtime", "room-ktx", "room-compiler"]
    - path: "app/build.gradle.kts"
      provides: "Module build file without Room deps or KSP arg"
      not_contains: ["room.schemaLocation", "androidx-room"]
    - path: "app/src/main/java/com/example/semora/SemoraApplication.kt"
      provides: "Clean Application class without Room/migration code"
      not_contains: ["AppDatabase", "runMigration", "applicationScope"]
  key_links:
    - from: "FirestoreProfileRepository.updateUsername"
      to: "EditProfileViewModel.save()"
      via: "call to updateUsername wraps exception in user-friendly message"
      pattern: "updateUsername"
    - from: "FirestoreProfileRepository.updatePin"
      to: "ChangePinViewModel.verifyCurrentPin() and updatePin()"
      via: "call to updatePin wraps exception in user-friendly message"
      pattern: "updatePin"
---

<objective>
Remove dead code (preload system, Room database, migration logic, KSP processor) and fix profile-edit crash by changing `.update()` to `.set(SetOptions.merge())`.

**Purpose:** Eliminate crash surface from preload race conditions, Room migration complexity, and Firestore .update() failures on missing documents. Reduce APK size and build complexity.

**Output:** Clean repositories, gradle config, and Application class. Room-free build that installs and runs identically.
</objective>

<execution_context>
@C:/Users/praka/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/praka/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-stability-hardening/01-CONTEXT.md
@.planning/phases/01-stability-hardening/01-RESEARCH.md
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove preload/consumePreloaded system and dead MutableStateFlow</name>
  <files>
    app/src/main/java/com/prakash/semora/data/remote/FirestoreSemesterRepository.kt
    app/src/main/java/com/prakash/semora/ui/auth/PinVerificationActivity.kt
    app/src/main/java/com/prakash/semora/ui/home/HomeViewModel.kt
  </files>
  <action>
    **FirestoreSemesterRepository.kt** (per D-18):
    - Delete the `@Volatile var preloadedSemesters` field (lines 26-28)
    - Delete `suspend fun preload(...)` method (lines 30-36)
    - Delete `fun consumePreloaded(): ...` method (lines 38-42)
    - Keep everything else intact — `getSemesters()`, `getSemester()`, `updateGradeField()`, `updateSemesterSummary()`, `saveSemester()`, `deleteSemester()`, `deleteAllSemesters()`, helper methods, and data classes.

    **PinVerificationActivity.kt** (per D-18):
    - Remove the call `FirestoreSemesterRepository.preload(deviceUid, profile.id)` at approximately line 137 (inside the `lifecycleScope.launch` block after `updateLastOpened`).
    - Keep the `lifecycleScope.launch` block and the `updateLastOpened` call; remove only the preload line.

    **HomeViewModel.kt** (per D-18, D-17):
    - Delete the `consumePreloaded` check block (approx lines 61-68) — the entire `if (preloaded != null) { ... return@launch }` block.
    - Verify there is no unused `MutableStateFlow<HomeDashboardData>` field. If one exists at field level, remove it. The current file uses `MutableLiveData` for `_dashboard` — confirm no dead StateFlow field lingers.
    - After removal, `loadDashboard()` should flow directly from the cached-dashboard check to the `try { getSemesters() }` block.
  </action>
  <verify>
    <automated>
      grep -v '^#' "app/src/main/java/com/prakash/semora/data/remote/FirestoreSemesterRepository.kt" | grep -c "preloadedSemesters|fun preload|fun consumePreloaded"
      if [ $? -eq 0 ]; then echo "FAIL: preload remnants found"; exit 1; fi
      grep -c "FirestoreSemesterRepository.preload" "app/src/main/java/com/prakash/semora/ui/auth/PinVerificationActivity.kt"
      if [ $? -eq 0 ]; then echo "FAIL: preload call still in PinVerificationActivity"; exit 1; fi
      grep -c "consumePreloaded" "app/src/main/java/com/prakash/semora/ui/home/HomeViewModel.kt"
      if [ $? -eq 0 ]; then echo "FAIL: consumePreloaded still in HomeViewModel"; exit 1; fi
      echo "PASS: All preload code removed"
    </automated>
  </verify>
  <done>
    - FirestoreSemesterRepository has no preload/consumePreloaded methods or preloadedSemesters field
    - PinVerificationActivity has no preload() call
    - HomeViewModel has no consumePreloaded() reference and no dead MutableStateFlow
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Room dependencies, source code, and migration logic</name>
  <files>
    gradle/libs.versions.toml
    app/build.gradle.kts
    build.gradle.kts
    app/src/main/java/com/prakash/semora/data/local/AppDatabase.kt
    app/src/main/java/com/prakash/semora/data/local/UserDao.kt
    app/src/main/java/com/prakash/semora/data/local/SemesterDao.kt
    app/src/main/java/com/prakash/semora/data/local/GradeDao.kt
    app/src/main/java/com/example/semora/SemoraApplication.kt
  </files>
  <action>
    **gradle/libs.versions.toml** (per D-13):
    - Comment out lines 28-30 (the three Room library entries: `androidx-room-runtime`, `androidx-room-ktx`, `androidx-room-compiler`).
    - Prepend `# ` to each line. Keep the lines visible but inert.

    **app/build.gradle.kts** (per D-13, D-15):
    - Line 3: Comment out `alias(libs.plugins.google.devtools.ksp)`.
    - Lines 35-37: Comment out the entire `ksp { arg("room.schemaLocation", ...) }` block.
    - Lines 47-50: Comment out the entire `// Room` dependency block (the `implementation(libs.androidx.room.runtime)`, `implementation(libs.androidx.room.ktx)`, `ksp(libs.androidx.room.compiler)` lines). Keep the `// Room` comment as-is so intent is clear.
    - Prepend `// ` to each line. Do not delete — only comment.

    **build.gradle.kts** (root) (per D-15):
    - Line 4: Comment out `alias(libs.plugins.google.devtools.ksp) apply false`.

    **app/src/main/java/com/prakash/semora/data/local/** (per D-14):
    - Delete the entire directory and all 4 files: `AppDatabase.kt`, `UserDao.kt`, `SemesterDao.kt`, `GradeDao.kt`.

    **SemoraApplication.kt** (per D-14, D-15):
    - Remove `import com.prakash.semora.data.local.AppDatabase` (line 10).
    - Remove `import kotlinx.coroutines.CoroutineScope` (line 14) and `import kotlinx.coroutines.Dispatchers` (line 15) and `import kotlinx.coroutines.SupervisorJob` (line 16) — only if `applicationScope` is the only user. Keep them if used elsewhere.
    - Remove the `private val applicationScope = CoroutineScope(SupervisorJob() + Dispatchers.IO)` field (line 22).
    - Remove the `initFirebase()` method call from `onCreate()` — replace `initFirebase()` with `lifecycleScope.launch { ... ensureSignedIn() }` directly or simply call `FirestoreAuthRepository.ensureSignedIn()` without the applicationScope wrapper. The simplest approach: replace `initFirebase()` in `onCreate` with a direct call that still signs in but without the migration overhead.
    - Remove the entire `initFirebase()` method (lines 45-52).
    - Remove the entire `runMigration()` method (lines 54-134).
    - Remove the `import com.prakash.semora.data.remote.ProfileDoc` if it was only used by `runMigration()`. Keep it if used elsewhere (check).
    - Remove `import kotlinx.coroutines.launch` and `import kotlinx.coroutines.tasks.await` if no longer needed — keep `FirestoreAuthRepository.ensureSignedIn()` call, which uses `await()` transitively. If the inline sign-in call uses `lifecycleScope.launch`, keep the import.
  </action>
  <verify>
    <automated>
      grep -c "androidx-room" "gradle/libs.versions.toml"
      if [ $? -eq 0 ]; then
        # Should only be commented
        grep -c "^androidx-room" "gradle/libs.versions.toml"
        if [ $? -eq 0 ]; then echo "FAIL: uncommented Room entry in libs.versions.toml"; exit 1; fi
      fi
      grep -c "room.schemaLocation\|androidx.room" "app/build.gradle.kts"
      if [ $? -eq 0 ]; then
        grep -E "^[[:space:]]*(implementation|ksp)\(" "app/build.gradle.kts" | grep -c "room"
        if [ $? -eq 0 ]; then echo "FAIL: uncommented Room dep in build.gradle.kts"; exit 1; fi
      fi
      Test-Path "app/src/main/java/com/prakash/semora/data/local/AppDatabase.kt"
      if ($?) { echo "FAIL: data/local/ still exists"; exit 1 }
      grep -c "AppDatabase\|runMigration\|applicationScope" "app/src/main/java/com/example/semora/SemoraApplication.kt"
      if [ $? -ne 0 ]; then echo "FAIL: Room remnants in SemoraApplication"; exit 1; fi
      echo "PASS: Room fully removed"
    </automated>
  </verify>
  <done>
    - Room library entries in libs.versions.toml are commented out
    - Room deps, KSP plugin, and schemaLocation in app/build.gradle.kts are commented out
    - KSP plugin in root build.gradle.kts is commented out
    - data/local/ directory and all 4 files deleted
    - SemoraApplication has no Room import, applicationScope, initFirebase(), or runMigration()
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix profile-edit crash — change .update() to .set(SetOptions.merge())</name>
  <files>
    app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt
  </files>
  <action>
    **FirestoreProfileRepository.kt** (per D-07, D-12, RESEARCH profile-edit fix):
    - Add `import com.google.firebase.firestore.SetOptions` at the top.
    - Change `updateUsername()` method (lines 48-51): replace the `.update("username", username)` call with `.set(mapOf("username" to username), SetOptions.merge())`. This creates the document if it doesn't exist instead of crashing with NOT_FOUND.
    - Change `updatePin()` method (lines 53-60): replace the `.update(mapOf(...))` call with `.set(mapOf("pinHash" to pinHash, "pinSalt" to pinSalt, "pinVersion" to pinVersion), SetOptions.merge())`.
    - Do NOT change `updateLastOpened()` or `deleteProfile()` — updateLastOpened is only called immediately after profile creation (doc always exists), and deleteProfile doesn't crash on missing doc.
    - Do NOT change `createProfile()` — it already uses `.set()` correctly.

    The callers (EditProfileViewModel.save(), ChangePinViewModel.verifyCurrentPin()/updatePin()) already have try/catch blocks wrapping the calls, so they'll handle any remaining Firestore exceptions with user-friendly messages.
  </action>
  <verify>
    <automated>
      grep -c "SetOptions.merge" "app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt"
      if [ $? -ne 0 ]; then echo "FAIL: SetOptions.merge not found in ProfileRepository"; exit 1; fi
      grep -c "\.update(" "app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt"
      if [ $? -ne 0 ]; then
        # updateLastOpened still uses .update() — that's OK
        grep -c "updateLastOpened" "app/src/main/java/com/prakash/semora/data/remote/FirestoreProfileRepository.kt"
        if [ $? -eq 0 ]; then echo "PASS: only updateLastOpened still uses .update() as expected"; exit 0; fi
        echo "FAIL: no .update() calls found but updateLastOpened wasn't expected"; exit 1
      fi
      echo "PASS: Profile repository uses .set(merge()) for updateUsername and updatePin"
    </automated>
  </verify>
  <done>
    - FirestoreProfileRepository.updateUsername uses .set(merge()) instead of .update()
    - FirestoreProfileRepository.updatePin uses .set(merge()) instead of .update()
    - updateLastOpened() remains .update() (safe — always called after profile creation)
    - import com.google.firebase.firestore.SetOptions added
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Firestore | Firestore calls cross network boundary; transient failures expected |
| Room DB (being removed) | Local SQLite database — deletion removes this trust boundary entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | gradle/libs.versions.toml | accept | Commenting lines (not deleting) preserves intent for future readers; worst case is stale comments |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced — only removals and edits. No install gate needed. |
</threat_model>

<verification>
1. Build compiles: `./gradlew assembleDebug` passes (Room references removed, KSP not required)
2. No Room references in any remaining source file
3. FirestoreProfileRepository uses SetOptions.merge() for updateUsername and updatePin
4. No preload/consumePreloaded calls exist in any file
</verification>

<success_criteria>
- App builds and runs without Room dependencies
- Preload system completely removed (no preload/consumePreloaded anywhere)
- Profile username and PIN updates use .set(merge()) — no crash on missing doc
- Migration code removed from SemoraApplication
- Build time improves slightly (KSP processor removed)
</success_criteria>
