---
phase: 02-ui-polish-m3-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/themes.xml
  - app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
  - app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt
  - app/src/main/java/com/prakash/semora/ui/profile/ManageProfilesActivity.kt
  - app/src/main/java/com/prakash/semora/ui/profile/EditProfileActivity.kt
  - app/src/main/java/com/example/semora/BaseActivity.kt
  - app/src/main/java/com/example/semora/SplashActivity.kt
  - app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
  - .planning/codebase/STRUCTURE.md
  - app/src/main/java/com/prakash/semora/ui/utils/ShimmerDrawable.kt (new)
  - app/src/main/res/drawable/bg_nav_pill.xml (new)
  - app/src/main/res/anim/slide_in_right.xml (new)
  - app/src/main/res/anim/slide_out_left.xml (new)
  - app/src/main/res/anim/slide_in_left.xml (new)
  - app/src/main/res/anim/slide_out_right.xml (new)
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 19 layouts use consistent M3 theme attributes (colorSurface, textAppearance, shapeAppearance)"
    - "All 6 dialogs share a common M3 style — no visual inconsistencies between dialog types"
    - "EditProfileActivity discard-changes dialog uses MaterialAlertDialogBuilder (not AlertDialog.Builder)"
    - "Edge-to-edge enables on all screens — content draws behind system bars with correct inset padding"
    - "Keyboard pushes content up on register, edit-profile, and PIN screens"
    - "ShimmerDrawable class exists and can be applied to skeleton layouts"
    - "Bottom nav pill drawable exists and anim resources are defined"
    - "STRUCTURE.md layout index matches the actual 19 files on disk"
  artifacts:
    - path: "app/src/main/res/values/themes.xml"
      provides: "M3 dialog theme overlay (standard + destructive)"
      contains: "ThemeOverlay.Semora.Dialog"
    - path: "app/src/main/java/com/prakash/semora/ui/utils/ShimmerDrawable.kt"
      provides: "Custom shimmer animation drawable"
      min_lines: 60
    - path: "app/src/main/res/drawable/bg_nav_pill.xml"
      provides: "Bottom nav pill indicator shape"
    - path: "app/src/main/res/anim/slide_in_right.xml"
      provides: "Tab slide-in from right"
    - path: "app/src/main/java/com/example/semora/BaseActivity.kt"
      provides: "enableEdgeToEdge + windowInsets listener + keyboard handling"
  key_links:
    - from: "BaseActivity.kt"
      to: "SplashActivity.kt, ProfilePickerActivity.kt"
      via: "Both extend BaseActivity instead of AppCompatActivity"
      pattern: "BaseActivity\\(\\)"
    - from: "themes.xml"
      to: "ProfileFragment.kt, SemesterFragment.kt, ManageProfilesActivity.kt, EditProfileActivity.kt"
      via: "Dialog call sites reference R.style.ThemeOverlay_Semora_Dialog"
      pattern: "ThemeOverlay_Semora_Dialog"
---

<objective>
Set up the foundational M3 infrastructure: dialog theme styling, edge-to-edge system bar handling across all activities, keyboard window inset animation, custom shimmer drawable, bottom nav pill drawable, and tab transition animation XML resources. Also migrate the lone AlertDialog.Builder call to MaterialAlertDialogBuilder and fix the stale STRUCTURE.md layout index.

Purpose: Create all the base components that Plans 02-02 and 02-03 will build upon. By the end of this plan, the theme system, animation resources, and architectural foundations are in place.
Output: themes.xml with dialog overlay styles, BaseActivity with edge-to-edge + keyboard, ShimmerDrawable.kt, bg_nav_pill.xml, 4 anim XML resources, fixed STRUCTURE.md, migrated EditProfileActivity dialog.
</objective>

<execution_context>
@C:/Users/praka/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/praka/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/PATTERNS.md
@.planning/phases/01-stability-hardening/01-02-SUMMARY.md

# Key source files for context
@app/src/main/res/values/themes.xml
@app/src/main/res/values/colors.xml
@app/src/main/res/values/dimens.xml
@app/src/main/res/values/strings.xml
@app/src/main/java/com/example/semora/BaseActivity.kt
@app/src/main/java/com/example/semora/MainActivity.kt
@app/src/main/java/com/example/semora/SplashActivity.kt
@app/src/main/java/com/prakash/semora/ui/profile/EditProfileActivity.kt
@app/src/main/java/com/prakash/semora/ui/profile/ManageProfilesActivity.kt
@app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt
@app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
@app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add M3 dialog theme overlay + migrate AlertDialog.Builder + fix STRUCTURE.md</name>
  <read_first>
    app/src/main/res/values/themes.xml
    app/src/main/java/com/prakash/semora/ui/profile/EditProfileActivity.kt
    app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt
    app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
    app/src/main/java/com/prakash/semora/ui/profile/ManageProfilesActivity.kt
    .planning/codebase/STRUCTURE.md
  </read_first>
  <files>
    app/src/main/res/values/themes.xml,
    app/src/main/java/com/prakash/semora/ui/profile/EditProfileActivity.kt,
    app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt,
    app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt,
    app/src/main/java/com/prakash/semora/ui/profile/ManageProfilesActivity.kt,
    .planning/codebase/STRUCTURE.md
  </files>
  <action>
    Add dialog theme overlay to themes.xml:
    - Create `<style name="ThemeOverlay.Semora.Dialog" parent="ThemeOverlay.Material3.Dialog">` with:
      - `colorSurface` → `@color/md3_surface`
      - `shapeAppearance` → `@style/ShapeAppearance.Semora.MediumComponent`
      - `colorOnSurface` → `@color/md3_on_surface`
      - `colorOnSurfaceVariant` → `@color/md3_on_surface_variant`
    - Create `<style name="ThemeOverlay.Semora.Dialog.Destructive" parent="ThemeOverlay.Semora.Dialog">` with:
      - `buttonBarPositiveButtonStyle` → `@style/Widget.Semora.Dialog.DestructiveButton` (new style)
      - Create `Widget.Semora.Dialog.DestructiveButton` style referencing `Widget.Material3.Button.TextButton` with `android:textColor` → `@color/md3_error` and `backgroundTint` → `@color/md3_error_container`
      - Create `Widget.Semora.Dialog.PositiveButton` and `Widget.Semora.Dialog.NegativeButton` styles as needed

    In EditProfileActivity.kt (per D-15, D-16):
    - Change `AlertDialog.Builder` import to `com.google.android.material.dialog.MaterialAlertDialogBuilder`
    - Replace `AlertDialog.Builder(this@EditProfileActivity)` with `MaterialAlertDialogBuilder(this@EditProfileActivity, R.style.ThemeOverlay_Semora_Dialog)`
    - Keep all dialog callbacks unchanged

    In ProfileFragment.kt (per D-15):
    - In `showThemePicker()`: change `MaterialAlertDialogBuilder(requireContext())` to `MaterialAlertDialogBuilder(requireContext(), R.style.ThemeOverlay_Semora_Dialog)`
    - In `showLogoutConfirm()`: change to `MaterialAlertDialogBuilder(requireContext(), R.style.ThemeOverlay_Semora_Dialog)` — but this is a destructive action per D-16, change to `MaterialAlertDialogBuilder(requireContext(), R.style.ThemeOverlay_Semora_Dialog_Destructive)`

    In SemesterFragment.kt (per D-16):
    - In `showResetDialog()`: change to `MaterialAlertDialogBuilder(requireContext(), R.style.ThemeOverlay_Semora_Dialog_Destructive)` — reset is destructive

    In ManageProfilesActivity.kt (per D-16):
    - In `showDeleteConfirmDialog()`: change to `MaterialAlertDialogBuilder(this, R.style.ThemeOverlay_Semora_Dialog_Destructive)` — delete is destructive

    Fix STRUCTURE.md layout index:
    - Replace the stale 19-file list (which includes non-existent files like `item_semester_card.xml`, `dialog_delete_semester.xml`, etc.) with the actual inventory verified in RESEARCH.md:
      activity_splash.xml, activity_main.xml, activity_profile_picker.xml, activity_register.xml, activity_pin_verification.xml, activity_change_pin.xml, activity_edit_profile.xml, activity_manage_profiles.xml, fragment_home.xml, fragment_semester.xml, fragment_profile.xml, item_profile.xml, item_manage_profile.xml, item_create_profile.xml, item_semester_pill.xml, item_subject.xml, bottom_sheet_grade_picker.xml, bottom_sheet_avatar_options.xml, dialog_pin_verify.xml
    - Remove the stale descriptions for non-existent files and add descriptions for the actual files
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; if (Test-Path ".planning/codebase/STRUCTURE.md") { Write-Output "STRUCTURE.md exists" }; Select-String -Path "app/src/main/res/values/themes.xml" -Pattern "ThemeOverlay.Semora.Dialog" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    themes.xml contains ThemeOverlay.Semora.Dialog and ThemeOverlay.Semora.Dialog.Destructive styles. EditProfileActivity uses MaterialAlertDialogBuilder. ProfileFragment (2 calls), SemesterFragment (1 call), ManageProfilesActivity (1 call) all reference dialog theme overlay styles. STRUCTURE.md lists exactly the 19 real layout files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ShimmerDrawable + bg_nav_pill + anim XML resources</name>
  <read_first>
    app/src/main/res/values/dimens.xml
    app/src/main/res/values/colors.xml
    app/src/main/res/drawable/bg_active_dot.xml
    app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt
    app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
    app/src/main/res/layout/fragment_home.xml
  </read_first>
  <files>
    app/src/main/java/com/prakash/semora/ui/utils/ShimmerDrawable.kt (new),
    app/src/main/res/drawable/bg_nav_pill.xml (new),
    app/src/main/res/anim/slide_in_right.xml (new),
    app/src/main/res/anim/slide_out_left.xml (new),
    app/src/main/res/anim/slide_in_left.xml (new),
    app/src/main/res/anim/slide_out_right.xml (new)
  </files>
  <action>
    Create ShimmerDrawable.kt in `com.prakash.semora.ui.utils` package:
    - Class `ShimmerDrawable : android.graphics.drawable.Drawable()` with:
      - `private val paint = Paint(Paint.ANTI_ALIAS_FLAG)` 
      - `private val animator = ValueAnimator.ofFloat(0f, 1f).apply { duration = 1200L; repeatCount = INFINITE; repeatMode = RESTART; interpolator = AccelerateDecelerateInterpolator(); addUpdateListener { invalidateSelf() } }`
      - `private var shimmerFraction = 0f` (updated from animator)
      - `override fun draw(canvas)` — draws a `LinearGradient` at translated x-position based on animated fraction, using colors `0x00FFFFFF`, `0x33FFFFFF`, `0x00FFFFFF`. The gradient sweeps from `-width + (2f * width * fraction)` to `width + (2f * width * fraction)`.
      - `override fun setAlpha(alpha: Int) { paint.alpha = alpha }`
      - `override fun setColorFilter(filter: ColorFilter?) { paint.colorFilter = filter }`
      - `override fun getOpacity(): Int = PixelFormat.TRANSLUCENT`
      - `fun startShimmer() { animator.start() }` — ponytail: animator keeps running, setVisible/onAttachedToWindow for lifecycle
      - `fun stopShimmer() { animator.cancel() }`
      - Override `setVisible(visible, restart)` to call startShimmer/stopShimmer based on visibility

    Create bg_nav_pill.xml in res/drawable:
    - `<shape android:shape="rectangle">` with solid `@color/md3_primary_container` (per D-07 — filled pill) and `<corners android:radius="20dp" />`
    - Container width derived from icon (26dp) + label (varies) + 12dp padding each side. Use fixed width of 80dp to match bottom_nav_container / 3 per A2 assumption.

    Create 4 anim XML resources:
    - `slide_in_right.xml`: `<translate android:fromXDelta="100%" android:toXDelta="0%" android:duration="200" />`
    - `slide_out_left.xml`: `<translate android:fromXDelta="0%" android:toXDelta="-100%" android:duration="200" />`
    - `slide_in_left.xml`: `<translate android:fromXDelta="-100%" android:toXDelta="0%" android:duration="200" />`
    - `slide_out_right.xml`: `<translate android:fromXDelta="0%" android:toXDelta="100%" android:duration="200" />`
    - Each anim wrapped in `<set xmlns:android="...">` with the translate element
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; @(Get-ChildItem "app/src/main/res/anim/slide_*.xml").Count -eq 4 -and (Test-Path "app/src/main/java/com/prakash/semora/ui/utils/ShimmerDrawable.kt") -and (Test-Path "app/src/main/res/drawable/bg_nav_pill.xml")</automated>
  </verify>
  <done>
    ShimmerDrawable.kt compiles with ValueAnimator, draw(), setVisible lifecycle. bg_nav_pill.xml renders as 20dp-radius rectangle with primary_container fill. All 4 slide anim XMLs exist with correct direction/duration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add enableEdgeToEdge + keyboard handling to BaseActivity + fix activities to extend BaseActivity</name>
  <read_first>
    app/src/main/java/com/example/semora/BaseActivity.kt
    app/src/main/java/com/example/semora/SplashActivity.kt
    app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
    app/src/main/res/layout/activity_profile_picker.xml
    app/src/main/res/layout/activity_register.xml
    app/src/main/res/layout/activity_splash.xml
  </read_first>
  <files>
    app/src/main/java/com/example/semora/BaseActivity.kt,
    app/src/main/java/com/example/semora/SplashActivity.kt,
    app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
  </files>
  <action>
    In BaseActivity.kt (per D-13, D-14):
    - Add imports: `import androidx.activity.enableEdgeToEdge`, `import androidx.core.view.ViewCompat`, `import androidx.core.view.WindowInsetsCompat`, `import androidx.core.view.WindowInsetsAnimationCompat`, `import androidx.core.view.WindowInsetsAnimationCompat.Callback`
    - In `onCreate()` (before `setContentView`): add `enableEdgeToEdge()` — this applies to all activities that extend BaseActivity (MainActivity, EditProfileActivity, ManageProfilesActivity, ChangePinActivity, PinVerificationActivity, RegisterActivity)
    - Add `ViewCompat.setOnApplyWindowInsetsListener` on the root content view class: set padding for system bars (left, top, right, bottom) from `insets.getInsets(WindowInsetsCompat.Type.systemBars())`
    - For keyboard handling (D-14): add `WindowCompat.setDecorFitsSystemWindows(window, false)` if not already set
    - Use `ViewCompat.setOnApplyWindowInsetsListener` with `WindowInsetsAnimationCompat` to listen for IME visibility changes and push content up by imeInsets.bottom. On the binding root view (or content view), when IME insets change, add bottom padding equal to imeInsets.bottom.
    - NOTE: BaseActivity doesn't have binding — it's abstract. Use `window.decorView.rootView` or `findViewById(android.R.id.content)` as the view to apply insets on.

    In SplashActivity.kt (per D-13):
    - Change `class SplashActivity : AppCompatActivity()` to `class SplashActivity : BaseActivity()`
    - No other changes needed — BaseActivity.onAttachBaseContext or similar not relevant

    In ProfilePickerActivity.kt (per D-13):
    - Change `class ProfilePickerActivity : AppCompatActivity()` to `class ProfilePickerActivity : BaseActivity()`
    - Remove `import androidx.appcompat.app.AppCompatActivity` (replace with BaseActivity import if not already)
    - No other changes needed

    Note: MainActivity already calls `enableEdgeToEdge()` in its own onCreate AND extends BaseActivity. After this change, MainActivity will have a double call (safe and idempotent). The redundant call will be removed in Plan 02-02 which already modifies MainActivity.kt.

    Verify all 19 layouts use consistent M3 theme attributes:
    - Check that all layouts use `@color/md3_background` or `?attr/colorSurface` for backgrounds
    - Check that all text views use `TextAppearance.Semora.*` styles
    - Check that all MaterialCardView buttons use M3 `Widget.Material3.Button.*` styles
    - This is a methodology task — document findings as comments. If any layout uses hardcoded colors (like `#FFFFFF`) or non-M3 text styles, flag them for Plan 02-03 to fix.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; Select-String -Path "app/src/main/java/com/example/semora/BaseActivity.kt" -Pattern "enableEdgeToEdge" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    BaseActivity calls enableEdgeToEdge() and has WindowInsetsCompat listener for system bars + IME. SplashActivity and ProfilePickerActivity extend BaseActivity. MainActivity's redundant enableEdgeToEdge() removed by Plan 02-02. Layout background/style audit documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No security-relevant changes — all modifications are visual/UI polish (dialog styling, edge-to-edge, shimmer drawables) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Dialog theme overlay | accept | Dialog theme styling has no security impact — visual only |
| T-02-02 | Tampering | ShimmerDrawable | accept | Custom drawable reads no user data, renders only gradient on canvas |
</threat_model>

<verification>
- `./gradlew assembleDebug` succeeds
- All dialog theme overlay styles present in themes.xml
- EditProfileActivity no longer uses AlertDialog.Builder
- BaseActivity has enableEdgeToEdge, windowInsets, and IME listener
- SplashActivity and ProfilePickerActivity compile as BaseActivity subclasses
- ShimmerDrawable.kt, bg_nav_pill.xml, and 4 anim XMLs exist
- STRUCTURE.md lists 19 actual layout files
</verification>

<success_criteria>
Plan 02-01 complete when all 3 tasks are done and `./gradlew assembleDebug` compiles cleanly. Dialog theme overlay, edge-to-edge architecture, and all reusable drawable/animation resources are ready for Plans 02-02 and 02-03.
</success_criteria>

<output>
Create `.planning/phases/02-ui-polish-m3-states/02-01-SUMMARY.md` when done
</output>
