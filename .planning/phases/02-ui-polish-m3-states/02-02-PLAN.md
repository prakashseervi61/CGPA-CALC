---
phase: 02-ui-polish-m3-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/layout/activity_main.xml
  - app/src/main/java/com/example/semora/MainActivity.kt
  - app/src/main/res/drawable/bg_active_dot.xml (delete)
autonomous: true
requirements: []
must_haves:
  truths:
    - "Bottom nav active tab shows filled pill/chip around icon+label instead of 6dp dot"
    - "Pill indicator slides to new position when switching tabs (150-200ms ease)"
    - "Each tab item shows M3 ripple on touch"
    - "Tab fragment transitions slide horizontally based on tab order"
    - "Semester card → semester tab uses fade-in transition"
    - "Other navigation paths (auth flow, dialogs, profile creation) have no animation"
    - "No double-animation feel — pill animation starts before fragment transition"
  artifacts:
    - path: "app/src/main/res/layout/activity_main.xml"
      provides: "Bottom nav with pill indicator container and ripple per tab"
      min_lines: 180
    - path: "app/src/main/java/com/example/semora/MainActivity.kt"
      provides: "Pill indicator animation + NavController NavOptions for tab transitions"
      exports: ["setupBottomNav", "selectTab", "updateTabStates"]
  key_links:
    - from: "activity_main.xml"
      to: "res/drawable/bg_nav_pill.xml"
      via: "Pill View background set to @drawable/bg_nav_pill"
      pattern: "bg_nav_pill"
    - from: "MainActivity.kt"
      to: "res/anim/slide_*.xml"
      via: "NavOptions.Builder references R.anim.slide_in_right, etc."
      pattern: "R\\.anim\\.slide_"
---

<objective>
Replace the current bottom-nav dot indicator with an M3-filled pill/chip that slides between tabs on selection. Add M3 default ripple touch feedback to each tab. Implement horizontal slide transitions between tab fragments via NavController NavOptions. Add fade-in transition for semester card → semester tab navigation.

Purpose: Deliver D-07 (pill indicator), D-08 (pill animation), D-09 (ripple), D-10 (tab slide), D-11 (fade-in), and D-12 (no other animations). Per RESEARCH.md (Pitfall 1), sequence the pill animation before the fragment transition to avoid visual competition.
Output: activity_main.xml with pill UI + MainActivity.kt with animation logic.
</objective>

<execution_context>
@C:/Users/praka/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/praka/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/res/layout/activity_main.xml
@app/src/main/java/com/example/semora/MainActivity.kt
@app/src/main/res/drawable/bg_active_dot.xml
@app/src/main/res/drawable/bg_nav_pill.xml (created in Plan 02-01)
@app/src/main/res/anim/slide_in_right.xml (created in Plan 02-01)
@app/src/main/res/values/colors.xml
@app/src/main/res/values/dimens.xml

# Prior phase summary for context on existing patterns
@.planning/phases/01-stability-hardening/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update bottom nav layout — replace dots with pill container + add ripple</name>
  <read_first>
    app/src/main/res/layout/activity_main.xml
    app/src/main/res/drawable/bg_active_dot.xml
    app/src/main/res/drawable/bg_nav_pill.xml
    app/src/main/res/values/dimens.xml
    app/src/main/res/values/colors.xml
  </read_first>
  <files>
    app/src/main/res/layout/activity_main.xml,
    app/src/main/res/drawable/bg_active_dot.xml (delete)
  </files>
  <action>
    In activity_main.xml (per D-07, D-09):
    - Remove the 6dp dot View (`android:id="@+id/dot_home"`, `dot_semester`, `dot_profile`) and its background `@drawable/bg_active_dot` from all 3 tabs
    - Add a new `View android:id="@+id/nav_pill"` inside `bottom_nav_container` (as a direct child of the ConstraintLayout, positioned behind the tab items) with:
      - `android:layout_width="0dp"` (width constrained to tab width via layout_constraints)
      - `android:layout_height="44dp"` (per RESEARCH.md recommendation: 44dp pill height)
      - `android:background="@drawable/bg_nav_pill"`
      - Constraint: start_toStartOf="parent", end_toEndOf="parent" initially — the pill's position is controlled programmatically in MainActivity.kt via `translationX`
      - `android:visibility="visible"` — starts visible on Home tab
      - `android:elevation="0dp"` (stays behind icon+label)
    - Add `android:foreground="?attr/selectableItemBackground"` to each tab FrameLayout (`nav_home`, `nav_semester`, `nav_profile`) for M3 ripple touch feedback per D-09
    - Deliver the `bg_active_dot.xml` file by deleting it (it's replaced by bg_nav_pill)

    Layout approach: The pill View sits in the ConstraintLayout with the same 0dp width + chain constraints as the tab items. On init, its position aligns with Tab 0 (Home). The Kotlin code translates it horizontally by tabWidth per index.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; $count = (Select-String -Path "app/src/main/res/layout/activity_main.xml" -Pattern "dot_home|dot_semester|dot_profile" | Measure-Object | Select-Object -ExpandProperty Count); if ($count -gt 0) { Write-Output "FAIL: dot indicators still present"; exit 1 } else { Write-Output "PASS: dots removed" }; $pill = (Select-String -Path "app/src/main/res/layout/activity_main.xml" -Pattern "nav_pill" | Measure-Object | Select-Object -ExpandProperty Count); if ($pill -gt 0) { Write-Output "PASS: nav_pill found" } else { Write-Output "FAIL: nav_pill not found"; exit 1 }</automated>
  </verify>
  <done>
    activity_main.xml no longer has dot_home/dot_semester/dot_profile Views. nav_pill View exists with bg_nav_pill drawable background. Each tab FrameLayout has selectableItemBackground foreground. bg_active_dot.xml deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement pill indicator animation + NavController tab transitions + fade-in</name>
  <read_first>
    app/src/main/java/com/example/semora/MainActivity.kt
    app/src/main/res/layout/activity_main.xml
  </read_first>
  <files>
    app/src/main/java/com/example/semora/MainActivity.kt
  </files>
  <action>
    In MainActivity.kt (per D-07, D-08, D-09, D-10, D-11, D-12):

    Remove redundant `enableEdgeToEdge()` call from `onCreate()` (BaseActivity now handles it).

    Update `setupBottomNav()`:
    - Change `dots` list to reference the pill View instead: `val pill = binding.navPill`
    - Pre-calculate `tabWidth` at the end of `setupBottomNav()`: `val tabWidth = binding.bottomNavContainer.width / 3` (post in a `post {}` block since width is 0 before layout pass)
    - Remove dot-related logic from the destination change listener
    - Add `tabWidth` as a class property or capture it in the listener closure

    Rewrite `updateTabStates()` (per D-07, D-08):
    - Replace the old dots visibility logic with pill translation
    - Remove the scale animation on icons (the pill replaces the visual indicator) but keep the icon tint change
    - Remove the label alpha animation (keep the text color change)
    - Add pill translation animation:
      ```kotlin
      val targetX = tabWidth * selected
      pill.animate()
          .translationX(targetX)
          .setDuration(175L)  // 150-200ms per D-08
          .setInterpolator(AccelerateDecelerateInterpolator())
          .start()
      ```

    Rewrite `selectTab()` (per D-10, D-11, D-12, and RESEARCH Pitfall 1):
    - Add `navController.navigate()` call with NavOptions that include slide animations per D-10:
      ```kotlin
      val anim = if (index > selectedIndex)
          R.anim.slide_in_right to R.anim.slide_out_left
      else
          R.anim.slide_in_left to R.anim.slide_out_right
      val options = NavOptions.Builder()
          .setEnterAnim(anim.first)
          .setExitAnim(anim.second)
          .setPopEnterAnim(R.anim.slide_in_left)
          .setPopExitAnim(R.anim.slide_out_right)
          .setLaunchSingleTop(true)
          .setRestoreState(true)
          .build()
      ```
    - Sequence per RESEARCH Pitfall 1: first call `updateTabStates()` (starts pill animation 175ms), then post-delay the `navController.navigate()` by 50ms so the pill starts moving before the fragment transition
    - Ensure `selectedIndex` is updated BEFORE `updateTabStates()` but the navigate uses the computed anim direction

    For semester card → semester tab fade-in (D-11, D-12):
    - When the tab transition is from Home (index 0) to Semester (index 1), use fade-in/fade-out anims instead of slide
    - Override the anim pair when `fromIndex == 0 && toIndex == 1`:
      ```kotlin
      if (fromIndex == 0 && toIndex == 1) {
          anim = R.anim.fade_in to R.anim.fade_out
      }
      ```
    - Note: `android.R.anim.fade_in` and `android.R.anim.fade_out` can be used directly, or define custom fade XMLs if consistency needed

    Update `switchToSemesterTab()`:
    - Ensure the animation logic in selectTab also applies when calling from outside
    - The binding.navPill reference needs to be available — it's already a binding member

    Remove unused imports:
    - Remove `dots` variable initialization if no longer needed
    - Keep `NavOptions` import
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; Select-String -Path "app/src/main/java/com/example/semora/MainActivity.kt" -Pattern "navPill" | Measure-Object | Select-Object -ExpandProperty Count; Write-Output "---flags---"; Select-String -Path "app/src/main/java/com/example/semora/MainActivity.kt" -Pattern "enableEdgeToEdge" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    MainActivity.kt uses View.navPill instead of View dots. Pill translates 175ms AccelerateDecelerate on tab switch. NavOptions.Builder references slide anim resources. Tab 0→1 uses fade anim. No enableEdgeToEdge in MainActivity. `./gradlew assembleDebug` succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Visual/UI changes only — no security-relevant modifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | None | Bottom nav animation | accept | Visual animation only, no data or auth impact |
</threat_model>

<verification>
- `./gradlew assembleDebug` succeeds
- nav_pill View present in activity_main.xml
- bg_active_dot.xml deleted
- MainActivity.kt references binding.navPill, uses NavOptions with slide anims
- Pill animation 175ms, fragment transition 200ms with 50ms offset
- No superfluous animation on other navigation paths (per D-12)
- `git diff` shows clean structural changes
</verification>

<success_criteria>
Bottom nav pill indicator replaces dot indicator. Tab transitions slide horizontally. Semester tab from home uses fade-in. Ripple on all tabs. No double-animation feel. Compiles cleanly.
</success_criteria>

<output>
Create `.planning/phases/02-ui-polish-m3-states/02-02-SUMMARY.md` when done
</output>
