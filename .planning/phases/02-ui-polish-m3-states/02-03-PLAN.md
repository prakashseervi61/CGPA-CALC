---
phase: 02-ui-polish-m3-states
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt
  - app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
  - app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt
  - app/src/main/res/layout/fragment_home.xml
  - app/src/main/res/layout/fragment_semester.xml
  - app/src/main/res/layout/fragment_profile.xml
  - app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
  - app/src/main/res/layout/activity_profile_picker.xml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Home/Profile/Semester loading states show shimmer animation overlaying skeleton cards"
    - "Shimmer animation stops when data loads (no infinite shimmer on visible content)"
    - "Skeleton card shapes match content card shapes (same rounded corners, proper heights)"
    - "Profile picker empty state shows text prompt with CTA button per D-03"
    - "Semester all-grades-empty state shows hint text per D-04"
    - "Error cards use M3-consistent styling (already error_container from Phase 1, verify polished)"
    - "HomeFragment semester-card click navigates to the Semester tab with transition"
  artifacts:
    - path: "app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt"
      provides: "Shimmer lifecycle wiring, semester card→semester tab navigation"
      min_lines: 130
    - path: "app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt"
      provides: "Shimmer lifecycle wiring, empty grade state visibility"
      min_lines: 240
    - path: "app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt"
      provides: "Shimmer lifecycle wiring"
      min_lines: 185
    - path: "app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt"
      provides: "Empty state text + CTA button wiring per D-03"
    - path: "app/src/main/res/layout/activity_profile_picker.xml"
      provides: "Empty state layout with updated text per D-03"
  key_links:
    - from: "HomeFragment.kt, SemesterFragment.kt, ProfileFragment.kt"
      to: "com.prakash.semora.ui.utils.ShimmerDrawable"
      via: "Set as background on loadingShimmer container or view tree"
      pattern: "ShimmerDrawable"
    - from: "ProfilePickerActivity.kt"
      to: "activity_profile_picker.xml"
      via: "emptyState visibility + btnEmptyCreate onClickListener"
---

<objective>
Integrate all state visuals: wire the ShimmerDrawable overlay into Home/Semester/Profile fragment loading states, polish skeleton card shapes, update profile picker empty state text per D-03, verify semester hint text per D-04, polish error card M3 styling, and hook up the semester card → semester tab navigation in HomeFragment.

Purpose: Deliver D-03 (profile picker empty state), D-04 (semester all-grades hint), D-05 (shimmer overlay on polished skeletons), and error card M3 polish. This plan completes the user-facing state coverage for the phase.
Output: All 3 fragments with shimmer overlay + polished skeleton shapes + updated empty/error states.
</objective>

<execution_context>
@C:/Users/praka/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/praka/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@app/src/main/res/layout/fragment_home.xml
@app/src/main/res/layout/fragment_semester.xml
@app/src/main/res/layout/fragment_profile.xml
@app/src/main/res/layout/activity_profile_picker.xml
@app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt
@app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
@app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt
@app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
@app/src/main/java/com/prakash/semora/ui/home/HomeViewModel.kt
@app/src/main/java/com/prakash/semora/ui/semester/SemViewModel.kt
@app/src/main/java/com/prakash/semora/ui/profile/ProfileViewModel.kt
@app/src/main/java/com/prakash/semora/ui/utils/ShimmerDrawable.kt (created in Plan 02-01)
@app/src/main/res/values/colors.xml
@app/src/main/res/values/strings.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire shimmer overlay into all 3 fragment loading states + update empty state texts</name>
  <read_first>
    app/src/main/java/com/prakash/semora/ui/utils/ShimmerDrawable.kt
    app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt
    app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt
    app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt
    app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt
    app/src/main/res/layout/activity_profile_picker.xml
    app/src/main/res/layout/fragment_semester.xml
    app/src/main/res/values/strings.xml
  </read_first>
  <files>
    app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt,
    app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt,
    app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt,
    app/src/main/java/com/prakash/semora/ui/auth/ProfilePickerActivity.kt,
    app/src/main/res/layout/activity_profile_picker.xml
  </files>
  <action>
    In all 3 fragment Kotlin files (HomeFragment.kt, SemesterFragment.kt, ProfileFragment.kt):
    - Add import: `import com.prakash.semora.ui.utils.ShimmerDrawable`
    - In `onViewCreated()` (or after binding inflate), create a ShimmerDrawable instance:
      ```kotlin
      private var shimmerDrawable: ShimmerDrawable? = null
      ```
    - In `onViewCreated()`, initialize and set as background on the loadingShimmer container:
      ```kotlin
      shimmerDrawable = ShimmerDrawable().apply {
          binding.loadingShimmer.background = this
      }
      ```
    - In the existing `observeLoading()` / `isLoading` observer: when loading becomes true, call `shimmerDrawable?.startShimmer()`. When loading becomes false, call `shimmerDrawable?.stopShimmer()`. Also when loading becomes true, set `binding.loadingShimmer.background = shimmerDrawable` (to ensure the drawable is attached).
    - In `onDestroyView()`: call `shimmerDrawable?.stopShimmer()` and set to null to prevent leaks (per RESEARCH Pitfall 3: shimmer lifecycle must be tied to view lifecycle).

    In HomeFragment.kt:
    - Pre-Ponytail note: The `observeLoading()` already toggles `binding.loadingShimmer.visibility` — add startShimmer/stopShimmer calls there. Also add `binding.cvAvatar`, `binding.cvSemesters`, `binding.cvCredits` placeholder shimmer in the skeleton — they already exist as MaterialCardView blocks in the loadingShimmer container.
    - Make the semester cards (cardSemesters, cardCredits) clickable to navigate to semester tab: add `binding.cardSemesters.setOnClickListener { (requireActivity() as? MainActivity)?.switchToSemesterTab() }` and `binding.cardCredits.setOnClickListener { ... }` in `setupListeners()`.

    In SemesterFragment.kt:
    - The `observeLoading()` already handles `isLoading` state — add shimmer start/stop there.

    In ProfileFragment.kt:
    - The `observeLoading()` already handles `isLoading` — add shimmer start/stop there.

    In ProfilePickerActivity.kt (per D-03):
    - Update the empty state title text: change `tvEmptyTitle.text = "No Profiles Yet"` (or whatever hardcoded text) — but actually it uses XML text, so update the XML. In the code, the empty state is shown via `showEmptyState()` which sets visibility. No Kotlin text change needed if XML handles it.
    - Ensure `btnEmptyCreate` CTA is wired — already is in `setupListeners()`.

    In activity_profile_picker.xml (per D-03):
    - Update the empty state TextView `tvEmptyTitle` text to "Create your first profile to get started" (replaces "No Profiles Yet" and the subtitle text)
    - Change the subtitle `tvEmptySubtitle` to a shorter/hidden state since the title is the primary prompt per D-03. The subtitle can remain as secondary context.
    - The `btnEmptyCreate` button already exists with correct label and listener.

    D-04 verification: The semester fragment already shows an empty state when `state.completedSubjects == 0` (line 155 in SemesterFragment.kt). The text "Tap a subject card and choose a grade to begin tracking your SGPA." already satisfies D-04's "Tap a course to add your grade" intent. No change needed — document in comments that D-04 is satisfied by existing implementation.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; Select-String -Path "app/src/main/java/com/prakash/semora/ui/home/HomeFragment.kt" -Pattern "ShimmerDrawable" | Measure-Object | Select-Object -ExpandProperty Count; Select-String -Path "app/src/main/java/com/prakash/semora/ui/semester/SemesterFragment.kt" -Pattern "ShimmerDrawable" | Measure-Object | Select-Object -ExpandProperty Count; Select-String -Path "app/src/main/java/com/prakash/semora/ui/profile/ProfileFragment.kt" -Pattern "ShimmerDrawable" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>
    All 3 fragments import and use ShimmerDrawable on loadingShimmer container. Shimmer starts on loading=true, stops on loading=false. Shimmer stops in onDestroyView. Profile picker empty state text updated per D-03. Semester empty state text verified per D-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish skeleton layouts + add smooth semester card nav + error card M3 polish</name>
  <read_first>
    app/src/main/res/layout/fragment_home.xml
    app/src/main/res/layout/fragment_semester.xml
    app/src/main/res/layout/fragment_profile.xml
    app/src/main/res/values/dimens.xml
    app/src/main/res/values/colors.xml
  </read_first>
  <files>
    app/src/main/res/layout/fragment_home.xml,
    app/src/main/res/layout/fragment_semester.xml,
    app/src/main/res/layout/fragment_profile.xml
  </files>
  <action>
    Per D-05 (polish skeleton layouts — correct card shapes, rounded corners, proper height with shimmer overlay):

    In fragment_home.xml loadingShimmer:
    - Hero card skeleton: already 140dp height with `@dimen/corner_large` — verify this matches the real hero card (line 186-291 shows real hero uses cardCornerRadius="@dimen/corner_large"). ✓ Already matches.
    - Stat card skeletons (2 cards, 100dp height): verify corner radius matches real stat cards (real cards use `@dimen/corner_medium`). Change skeleton to `app:cardCornerRadius="@dimen/corner_medium"`.
    - Section title skeleton (120dp×16dp, 8dp radius): verify shape matches real section title. The real "Statistics" title uses `TextAppearance.Semora.TitleSmall` with no card — this skeleton is a good approximation. Leave as-is.

    In fragment_semester.xml loadingShimmer (per D-05):
    - Semester pill skeleton (3 chips, 32dp height, 16dp radius): verify matches real chip `item_semester_pill.xml` (which has 16dp corner) — already matches.
    - Subject card skeletons (3 × 60dp height, `@dimen/corner_medium`): verify real subject cards `item_subject.xml` use the same corner radius. Keep matching — these are correct.
    - Semester title skeleton (160dp×20dp, 10dp radius): matches the "Semester Calculator" title placeholder. Fine.

    In fragment_profile.xml loadingShimmer (per D-05):
    - Avatar skeleton (72dp×72dp, 36dp radius): real avatar is 96dp×96dp, 48dp radius. Update skeleton to match: change to 96dp×96dp with `app:cardCornerRadius="48dp"`.
    - Username skeleton (100dp×14dp, 7dp radius): matches real username text size approximation. Fine.
    - Branch skeleton (140dp×12dp, 6dp radius): matches real branch text size. Fine.
    - Settings card skeleton (match_parent×160dp, `@dimen/corner_medium`): matches real settings cards in the scroll content. Fine.

    Final skeleton check — ensure ALL skeleton cards use the same corner radius values as their real-content counterparts. The shimmer overlay (applied via ShimmerDrawable in Task 1) will overlay these polished shapes.

    Error card M3 polish (per the agent's Discretion and D-01 for text-only):
    - Error cards already use `md3_error_container` background with `md3_error` icon tint and TextButton retry (from Phase 1).
    - Verify in all 3 fragment XMLs that error card uses `cardElevation="0dp"`, `contentPadding="12dp"`, 8dp margin — these are already correct.
    - Add `app:strokeWidth="0dp"` to error card if missing (ensures no outline on the card). Already present on fragment_home.xml error card? Let me check. Line 65: `app:cardElevation="0dp"` — no strokeWidth attribute. Add it.
    - In fragment_semester.xml, the error card at line 115-154: no `app:strokeWidth` either. Add it.
    - In fragment_profile.xml, the error card at line 24-63: no `app:strokeWidth`. Add it.

    M3 Theme Audit Walkthrough (agent's Discretion):
    - Scan the 19 layouts using the M3 theme audit checklist:
      ✓ All backgrounds use `@color/md3_background` or `?attr/colorSurface`
      ✓ All text uses `TextAppearance.Semora.*` styles
      ✓ All elevation references use `0dp` where cards are in scrollable containers
      ✓ No hardcoded colors except in grade chips (intentional)
      ✓ All dialog/layout margins use `@dimen/spacing_*` values
    - If any layout fails, fix it as part of this task. Prioritize the 3 main fragment layouts and activity_main.xml.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel 2>/dev/null || echo ".")" ; @(Select-String -Path "app/src/main/res/layout/fragment_profile.xml" -Pattern "96dp.*96dp" | Measure-Object | Select-Object -ExpandProperty Count); @(Select-String -Path "app/src/main/res/layout/fragment_home.xml" -Pattern "cardElevation=\"0dp\"" | Measure-Object | Select-Object -ExpandProperty Count)</automated>
  </verify>
  <done>
    All skeleton card dimensions and corner radii match real content cards. Profile avatar skeleton is 96dp×96dp with 48dp radius. Error cards in all 3 fragments have strokeWidth="0dp". M3 theme audit walkthrough completed with findings documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Visual/UI changes only — no security-relevant modifications. All changes are layout polish, shimmer animation, and state visibility toggles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | None | Shimmer overlay | accept | Visual only — no data read/write. Shimmer stops onDestroyView to prevent leak. |
</threat_model>

<verification>
- `./gradlew assembleDebug` succeeds
- HomeFragment, SemesterFragment, ProfileFragment all use ShimmerDrawable
- Shimmer starts/stops based on isLoading state
- Profile picker empty state text updated per D-03
- Semester empty state text verified per D-04
- Skeleton dimensions match real content
- Error cards have consistent M3 styling (strokeWidth="0dp", md3_error_container background)
</verification>

<success_criteria>
All 3 fragment loading states show shimmer animation. Shimmer correctly stops on data load. Skeleton shapes match real content dimensions. Profile picker shows D-03 text prompt + CTA. Semester shows hint text when no grades graded. Error cards have polished M3 styling. Compiles cleanly.
</success_criteria>

<output>
Create `.planning/phases/02-ui-polish-m3-states/02-03-SUMMARY.md` when done
</output>
