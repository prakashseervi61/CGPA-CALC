# Plan 03-02: Write All Unit Tests

**Phase:** 3 — Test Coverage
**Depends on:** 03-01-PLAN.md (infra + extraction)
**Goal:** All 7 test targets covered with specified edge cases

## Tasks

### 1. `SgpaCalculatorTest.kt`

**File:** `app/src/test/java/com/prakash/semora/utils/SgpaCalculatorTest.kt`

**Edge cases:**
- empty courses → 0.0
- zero credits (courses with 0 credits, all ungraded)
- all letter grades (verify weighted average)
- mix of graded and ungraded courses
- single course

### 2. `PinHasherTest.kt`

**File:** `app/src/test/java/com/prakash/semora/utils/PinHasherTest.kt`

**Edge cases:**
- hash + verify with valid PIN (round-trip)
- verify with wrong PIN → false
- verify with empty PIN
- hash with empty salt
- legacy v1 verify path (`pinVersion=0`)
- salt length mismatch handling

### 3. `SemesterCurriculumTest.kt`

**File:** `app/src/test/java/com/prakash/semora/data/SemesterCurriculumTest.kt`

**Edge cases:**
- `getSemester(1..8)` returns non-empty for each
- `getSemester(0)` → fallback SemesterData with empty list
- `getSemester(9)` → fallback
- `getSemester(99)` → fallback
- `gradeToPoint("O")` → 10.0, `"A+"` → 9.0, ..., `"U"` → 0.0
- `gradeToPoint("")` or `"Z"` → 0.0
- `totalCurriculumCredits` is positive

### 4. `HomeViewModelTest.kt`

**File:** `app/src/test/java/com/prakash/semora/ui/home/HomeViewModelTest.kt`

**Note:** Uses MockK to mock Application context.

**Edge cases:**
- empty semesters list → CGPA 0.0, 0 completed
- 1 semester with mixed grades → correct CGPA
- all courses graded → `completedSemesters` incremented
- no courses graded → 0 completed, CGPA 0.0
- zero graded credits → CGPA 0.0

### 5. `SemViewModelTest.kt`

**File:** `app/src/test/java/com/prakash/semora/ui/semester/SemViewModelTest.kt`

**Note:** Uses MockK for repo mock, default-param injection.

**Edge cases:**
- `updateGrade()` updates course grade → SGPA recomputed
- State immutability after update (original list unchanged)
- All courses graded → `allGraded=true`
- `selectSemester()` switches cached state
- Loading + error states

### 6. `FirestoreProfileRepositoryTest.kt`

**File:** `app/src/test/java/com/prakash/semora/data/remote/FirestoreProfileRepositoryTest.kt`

**Edge cases:**
- all fields present
- missing `pinHash` → defaults to `""`
- missing `pinSalt` → defaults to `""`
- null fields in map (type coercion)
- Long → Int coercion for numeric fields

### 7. `FirestoreSemesterRepositoryTest.kt`

**File:** `app/src/test/java/com/prakash/semora/data/remote/FirestoreSemesterRepositoryTest.kt`

**Edge cases:**
- missing `grades` field → empty map
- empty grades map → valid SemesterDoc
- null `semesterNumber` → 0
- type coercion (Long → Int for credits)
- null grade values in grades map

## Verification

- [ ] `./gradlew test` passes with all tests green
- [ ] Every edge case above is covered
